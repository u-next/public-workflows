# org-policies/.github/workflows/org-required.yml
name: Org Required

on:
  pull_request:
    types: [opened, synchronize, reopened]  # runs on all PR updates

jobs:
  required-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Minimal org check
        run: echo "✅ Org-wide check ran successfully!"
